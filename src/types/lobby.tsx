export type Lobby = {
    id: number;
    name: string;
    startTime: string;
    studyMin: number;
    breakMin: number;
    isPrivate: boolean;
}

export type LobbyCreationDate = {
    name: string;
    startTime: string;
    studyMin: number;
    breakMin: number;
    isPrivate: boolean;
}
