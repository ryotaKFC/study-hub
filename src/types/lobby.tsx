export type Lobby = {
    id: string;
    name: string;
    startTime: string;
    studyMin: number;
    breakMin: number;
    isPrivate: boolean;
}

export type LobbyCreationDate = {
    name: string;
    startTime: string;
    studyMin: number;
    breakMin: number;
    isPrivate: boolean;
}
