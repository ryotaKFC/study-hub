# Claude コードレビュー指示

このリポジトリのコードレビュー（GitHub Actions の `claude-code-review` ワークフロー、
および `@claude` メンション）で従う方針をまとめる。ローカルの Claude Code でも
プロジェクト共通の規約として参照する。

## 基本方針

- レビューコメントはすべて**日本語**で記述する
- 重大な問題（バグ・セキュリティ・パフォーマンス）を優先し、軽微なスタイル指摘は最小限にとどめる

## 命名規則

**ファイル名・変数名・関数名**は「リーダブルコード」の原則に従い、以下の点を確認する。

### 重点チェック項目

1. **目的が伝わる名前か**
   - `d`, `tmp2`, `data` などの意味が曖昧な名前は避ける
   - 何を表しているかが名前だけで分かること（例: `userCreatedAt`, `isEmailVerified`）

2. **誤解を招かない名前か**
   - `getXxx` は副作用のない取得のみ、変更を伴うなら `fetchXxx` / `updateXxx` など動詞を使い分ける
   - bool 変数は `is`, `has`, `can`, `should` などの接頭辞で意図を明示する

3. **スコープに合った粒度か**
   - 広いスコープで使われる変数ほど説明的な名前にする
   - **短命・即破棄の変数**（ループカウンタ `i` / `j`、分割代入の捨て変数 `_` など）は短い名前で構わない

4. **一貫性があるか**
   - 同じ概念には同じ単語を使う（`user` と `account` が混在しないか等）
   - ファイル名はディレクトリ内で命名規則が統一されているか

### 指摘しなくてよいケース

- ループインデックスの `i`, `j`, `k`
- 慣習的な省略形（`req`, `res`, `ctx`, `err` など）
- 即時破棄・スコープが1〜2行以内の一時変数

## プロジェクト固有のルール

### Next.js App Router

- `"use client"` は必要最小限に抑える（状態管理・イベントハンドラが必要な場合のみ）
- データフェッチは Server Component で行い、クライアントに不要なデータを渡さない

### Supabase

- ブラウザ側では `src/lib/supabase/client.ts` の `createClient()`、サーバー側では `src/lib/supabase/server.ts` の `createClient()` を使い分けているか確認する（同名だが import パスで区別する）
- 環境変数へのアクセスはブラケット記法 NG（`process.env['KEY']` ではなく `process.env.KEY`）
  — Next.js が `NEXT_PUBLIC_*` を静的インライン展開するため

### TypeScript

- 型インポートは `import type { ... }` で明示する
- `any` の多用や型アサーション（`as`）の乱用を指摘する。Supabase の生成型を活用する
- コンポーネント props に `Readonly<{...}>` を使うと再代入防止になる（推奨）

### Tailwind CSS v4

- `@apply` は使用しない（インラインユーティリティのみ）
- カスタム変数の追加は `globals.css` 内の `@theme inline {}` ブロックで定義する

### フォーマット（指摘不要）

インデント・クォート・import 順序は Biome が自動管理するため、これらのスタイルは指摘しない。

## その他のレビュー観点

- **型安全性**: TypeScript の `any` 多用や型アサーションの乱用を指摘する
- **セキュリティ**: 環境変数の露出、XSS / SQL インジェクションリスクを必ず確認する
- **可読性**: 1関数が長すぎる・ネストが深すぎる場合は分割を提案する
- **重複コード**: 同じロジックが複数箇所にあれば共通化を提案する
